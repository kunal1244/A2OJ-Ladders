#include <bits/stdc++.h>
using namespace std;
#define ll long long int

bool check(ll i, ll j, ll n, ll m){
}

void solve(){
    ll n, m;
    cin >> n >> m;
    vector<string> arr(n, "");
    for(ll i = 0; i < n; i++){
        cin >> arr[i];
    }

}
 
int main(){
    std::ios_base::sync_with_stdio(NULL);
	cin.tie(NULL); 
    cout.tie(NULL);
    ll t = 1;
    // cin >> t;
    while(t--){
        solve();
    }
    return 0;
}